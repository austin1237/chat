{
  "scripts": {
    "start": "node server.js",
    "test": "mocha test --exit && mocha integrationTests --exit"
  },
  "dependencies": {
    "express": "^4.17.1",
    "knex": "^0.19.5",
    "mysql": "^2.17.1",
    "socket.io": "^2.2.0",
    "socket.io-redis": "^5.2.0",
    "validator": "^11.1.0"
  },
  "devDependencies": {
    "chai": "4.2.0",
    "supertest": "4.0.2"
  }
}
